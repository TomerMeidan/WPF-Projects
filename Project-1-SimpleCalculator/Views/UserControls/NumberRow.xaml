<UserControl x:Class="Project_1_SimpleCalculator.Views.UserControls.NumberRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project_1_SimpleCalculator.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Button Name="btnFirst" Grid.Column="0" Content="{Binding FirstNumber}" Background="LightGray" />
        <Button Name="btnSecond" Grid.Column="1"  Content="{Binding SecondNumber}" Background="LightGray" />
        <Button Name="btnThird" Grid.Column="2"  Content="{Binding ThirdNumber}" Background="LightGray" />
    </Grid>
</UserControl>
